import React from 'react'
import { SafeAreaView, Text } from 'react-native'

const Saved = () => {
  return (
    <SafeAreaView>
      <Text>Saved</Text>
    </SafeAreaView>
  )
}

export default Saved